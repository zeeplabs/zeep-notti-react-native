// SPEC_DEVIATION: T4 replaced NativeNuntis.ts's scaffolded `multiply` method
// with the real v1 Spec, orphaning the `multiply`/`multiply.native` files
// (removed). The real public facade (Nuntis.initialize, User.addTag(s),
// login/logout, setSubscription, addEventListener) lands in T16 — this
// export is intentionally left minimal until then.
export {};
